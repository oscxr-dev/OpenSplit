# Copy to .env.local (dev) or provide at deploy time. Do NOT commit real values:
# .env.local and .env.production are gitignored.
VITE_API_BASE_URL=/api/v1
VITE_APP_NAME=OpenSplit
VITE_DEFAULT_CURRENCY=sats
